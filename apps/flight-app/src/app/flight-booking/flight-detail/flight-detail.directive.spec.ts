import { FlightDetailDirective } from './flight-detail.directive';

describe('FlightDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new FlightDetailDirective();
    expect(directive).toBeTruthy();
  });
});
